server:
  port: 8083
spring:
  cloud:
    config:
      server:
        git:
          default-label: master
          uri: https://github.com/EOrtega27/Spring-Store-Cloud.git
          searchPaths: config-data
          username: ${GIT_USER}
          password: ${GIT_PASSWORD}
  security:
    user:
      name: root
      password: s3cr3t
